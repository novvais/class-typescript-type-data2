type Email = string;

type Endereco = {
    quadra: string,
    cep: string,
    cidade: string
}

type Usuario = {
    nome: string, 
    idade: number, 
    email: Email, 
    cpf: string,
    endereco: Endereco
}

const cadastroT = ( info: Usuario ) => {
    
}

const cadastroT2 = ( info:  Usuario ) => {

}